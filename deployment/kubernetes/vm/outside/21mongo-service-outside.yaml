apiVersion: v1
kind: Service
metadata:
  name: mongo-service-outside
spec:
  ports:
  - protocol: TCP
    targetPort: 27017
    port: 30017
    nodePort: 30017
  type: NodePort
  selector:
    name: mongo
