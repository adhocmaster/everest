apiVersion: v1
kind: Service
metadata:
  name: mongo-outside-service
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - protocol: TCP
    targetPort: {{ .Values.containerPort }}
    port: {{ .Values.portOutside }}
    nodePort: {{ .Values.portOutside }}
  type: NodePort
  selector:
    app: mongo
